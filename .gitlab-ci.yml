Build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - echo $CI_ENV >> build/.env    
    - ls -la 
    - ls -la build    
  script:
    - ./script_staging.sh
